import { INITIAL_ROOMS, POPULAR_LOCATIONS, ROOM_TYPES, INITIAL_OWNERS } from './mockRooms';

export const rooms = INITIAL_ROOMS;
export const popularLocations = POPULAR_LOCATIONS;
export const roomTypes = ROOM_TYPES;
export const owners = INITIAL_OWNERS;

export default rooms;
